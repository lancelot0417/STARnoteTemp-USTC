% !TeX root = ./main.tex

% Project-specific shorthand commands belong in this file.
%
% Keep chapter files portable by defining analysis names, symbols, and repeated
% text here instead of inside chapters/*.tex, starnote.cls, or starphysics.sty.
% If the same chapters are copied into a ustcthesis project, copy this file too
% and load it from ustcsetup.tex or the thesis main preamble.
%
% For math symbols that may appear in normal text, wrap the definition in
% \ensuremath{...}. Use \providecommand for optional defaults and \renewcommand
% only when you intentionally override an existing command.

% Examples:
% \newcommand{\pT}{\ensuremath{p_{\mathrm{T}}}}
% \newcommand{\sNN}{\ensuremath{\sqrt{s_{_{\mathrm{NN}}}}}}
% \newcommand{\myObservable}{\ensuremath{R_{\mathrm{AA}}^{\mu\mu}}}
% \newcommand{\myCollisionSystem}{Au+Au collisions at \sNN\ = 200 GeV}
% \providecommand{\myDataset}{STAR Run 14}
